When interacting with GitHub, use the `gh` tool.

Application settings are stored in `$HOME/Library/Application\ Support/document-research/`.

If you need to use python, just use the `python` command, not `python3` or anything else.
