---
phase: 01-electron-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, .gitignore]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Electron dependencies installed"
    - "electron-vite configured"
    - "Package scripts configured for Electron development"
    - "Build artifacts excluded from git"
  artifacts:
    - path: "package.json"
      provides: "Electron dependencies and scripts"
      contains: ["electron", "electron-vite", "electron-builder", "electron-serve", "concurrently"]
    - path: ".gitignore"
      provides: "Electron build artifact exclusions"
      contains: ["out/", "release/", "dist/"]
  key_links: []
---

<objective>
Install Electron dependencies and configure package.json for desktop application development using electron-vite.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-electron-foundation/01-ELECTRON-VITE-RESEARCH.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Install Electron and electron-vite dependencies</name>
  <files>package.json</files>
  <action>
    Install Electron and electron-vite packages:
    - Run: `pnpm add --save-dev electron electron-vite vite`
    - Run: `pnpm add --save-dev electron-builder` (for packaging)
    - Run: `pnpm add --save-dev concurrently` (for running Next.js + Electron dev)
    - Run: `pnpm add electron-serve` (for serving static files in production)
    - Run: `pnpm add better-sqlite3` (needed for Phase 1-04)
    - Run: `pnpm add --save-dev typescript @types/node` (TypeScript support)

    Update package.json main entry to "out/main/index.js"

    Add electron-vite scripts to package.json:
      * "dev": "concurrently -n \"NEXT,ELECTRON\" -c \"yellow,blue\" --kill-others \"next dev\" \"electron-vite dev\""
      * "dev:next": "next dev"
      * "dev:electron": "electron-vite dev"
      * "build": "electron-vite build && next build"
      * "build:win": "npm run build && electron-builder --win"
      * "build:mac": "npm run build && electron-builder --mac"
      * "build:linux": "npm run build && electron-builder --linux"
      * "preview": "electron-vite preview"

    DO NOT modify existing Next.js scripts (dev, build, start, lint) - merge with them.
  </action>
  <verify>package.json contains electron, electron-vite, electron-builder, electron-serve, concurrently, better-sqlite3, typescript, @types/node in dependencies/devDependencies</verify>
  <done>All Electron packages installed, main entry configured to out/main/index.js, scripts added</done>
</task>

<task type="auto">
  <name>Update .gitignore for Electron artifacts</name>
  <files>.gitignore</files>
  <action>
    Add to .gitignore:
    - out/ (electron-vite build output)
    - release/ (electron-builder output)
    - dist/ (alternative build output)
    - *.log (Electron logs)

    Keep existing .next, node_modules, etc.
  </action>
  <verify>.gitignore contains out/, release/, dist/ entries</verify>
  <done>Build artifacts excluded from git</done>
</task>

</tasks>

<verification>
- Electron packages in package.json
- electron-vite dev/build scripts available
- Main entry points to out/main/index.js
- Build artifacts excluded from version control
</verification>

<success_criteria>
Electron dependencies and electron-vite installed and configured for development workflow.
</success_criteria>

<output>
After completion, create `.planning/phases/01-electron-foundation/01-01-SUMMARY.md`
</output>
